{
  "name": "budget",
  "version": "1.0.0",
  "description": "Budget project",
  "main": "app.js",
  "scripts": {
    "start": "export NODE_OPTIONS=--openssl-legacy-provider && nodemon app.js",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "concurrently": "^5.3.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mongoose": "^5.13.22",
    "nodemon": "^2.0.7",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "author": "",
  "license": "ISC"
}
